#include <bits/stdc++.h>
using namespace std;

// 그래프의 중위방문

int K;
vector<int> order;
vector<vector<int>> ret;

int main(){
    cin >> K;
    ret = vector<vector<int>>(K);

    int N;
    while(cin >> N) order.push_back(N);






    return 0;
}